/**
 * CV header component entry.
 *
 * Why:
 * - Re-export component + prop types from one place for clean imports.
 */
export { default } from './cvHeader.vue'
export type { Personal } from './cvHeader.types'
