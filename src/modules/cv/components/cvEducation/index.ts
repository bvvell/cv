/**
 * CV education component entry.
 *
 * Why:
 * - Renders Education + Courses side-by-side (responsive to 1 column).
 */
export { default } from './cvEducation.vue'
export type { Education, Course } from './cvEducation.types'
