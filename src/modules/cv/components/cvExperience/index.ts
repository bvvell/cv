/**
 * CV experience component entry.
 *
 * Why:
 * - Work history is the main CV content; this component owns the layout of roles + bullets + tags.
 */
export { default } from './cvExperience.vue'
export type { Experience } from './cvExperience.types'
